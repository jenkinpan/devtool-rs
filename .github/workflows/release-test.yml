name: Test Release Workflow

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Test step
        run: echo "Test successful"
